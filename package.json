{
  "name": "feedpets",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build:protocol": "pnpm --filter @feedpets/protocol build",
    "test:react": "pnpm --filter react-app exec vitest run",
    "build:react": "pnpm --filter react-app build",
    "ci": "pnpm build:protocol && pnpm test:react && pnpm build:react"
  }
}
